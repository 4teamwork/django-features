Add explicit matching of caller-supplied choices from one field and untranslated external_label metadata, reusable field/choice prefetching, and validation rules in field metadata. Every concrete custom-value consumer requires an AddField migration; optional field-scoped external-label uniqueness belongs to its concrete model. See the README upgrade and deployment guide. Matcher option errors raise ValueError; callers own key types, field scoping and preloading, while duplicate, missing and unhashable keys remain safe validation errors.
